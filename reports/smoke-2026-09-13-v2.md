# Investigation batch

3 questions, $0.78, 3 min.

Verdicts: 1 could_not_test, 1 inconclusive, 1 supported.

`Obs`/`Rep` are observed and reported ledger entries: observed means a command ran in the sandbox and printed it. `(v)` marks a verdict downgraded for lacking observed evidence.

| Question | Verdict | Obs | Rep | Blockers | Setup | Cost | Wall | Steps |
|---|---|---:|---:|---:|---:|---:|---:|---:|
| Is langfuse/langfuse actually open source under a recognised OSI licence, or is some of what its README advertises cover... | inconclusive | 0 | 6 | - | 0s | $0.203 | 54s | 5 |
| Does microsoft/apm actually turn an apm.yml manifest into configured agent files with a single command, without network ... | supported | 1 | 2 | - | 20s | $0.349 | 79s | 10 |
| Does 0xPlaygrounds/rig actually build and run a working LLM agent from its own quickstart example? | could_not_test | 1 | 3 | unsupported_language, needs_api_key | 8s | $0.226 | 49s | 6 |

## Trajectories

```
QUESTION  Is langfuse/langfuse actually open source under a recognised OSI licence, or is some of what its README advertises covered by a separate commercial licence?
REPO      langfuse/langfuse
CLAIM     Langfuse is an open source AI engineering platform.

TRAJECTORY
  - step 1: get_repo_metadata({'repo': 'langfuse/langfuse'})
  - step 1: read_file({'repo': 'langfuse/langfuse', 'path': 'README.md'})
  - step 2: list_files({'repo': 'langfuse/langfuse', 'path': ''})
  - step 2: read_file({'repo': 'langfuse/langfuse', 'path': 'LICENSE'})
  - step 3: list_files({'repo': 'langfuse/langfuse', 'path': 'ee'})
  - step 4: read_file({'repo': 'langfuse/langfuse', 'path': 'ee/LICENSE'})
  - step 4: read_file({'repo': 'langfuse/langfuse', 'path': 'ee/README.md'})

LEDGER
  [    for / reported] Langfuse is an open source LLM engineering platform. It helps teams collaboratively develop, monitor, evaluate, and debug AI applications.
            from: read_file(README.md)
  [against / reported] GitHub's own license detector could not assign a single recognised license to the repo, returning "license": "NOASSERTION" in the metadata rather than e.g. "MIT".
            from: get_repo_metadata(langfuse/langfuse)
  [against / reported] The root LICENSE file states: Copyright (c) 2023-2026 ClickHouse, Inc. Portions of this software are licensed as follows: All content that resides under the "ee/", "web/src/ee/", and/or "worker/src/ee/" directories of this repository, if these directories exist, is licensed under the license defined...
            from: read_file(LICENSE)
  [against / reported] The repo does in fact contain an ee/ directory, confirmed by listing the repository root and the ee folder contents (LICENSE, src, etc.).
            from: list_files(ee)
  [against / reported] The ee/LICENSE file states Langfuse is an open core project; its core is MIT-licensed, but certain periphery parts are commercially licensed under an Enterprise License requiring a valid agreement, and forbidding copying, merging, publishing, distributing, sublicensing, or selling the Software.
            from: read_file(ee/LICENSE)
  [against / reported] This folder includes features that are only available in the Enterprise Edition of Langfuse and on Langfuse Cloud.
            from: read_file(ee/README.md)

FACTS
  setup          0.0s over 0 command(s), install not run/failed
  disk           0 MB cloned, 0 MB total
  needs          nothing special
  exercised      -
  output         "license": "NOASSERTION"

VERDICT   INCONCLUSIVE
SUMMARY   The claim "Langfuse is an open source AI engineering platform" is half-true. The core of Langfuse (everything outside the ee/, web/src/ee/, and worker/src/ee/ directories) genuinely is MIT-licensed, an OSI-recognised open source license. But a non-trivial portion of the advertised feature set ("Enterprise Edition" features, available in both self-hosted and Cloud) is carved out and governed by a separate, non-OSI "Enterprise License" that is source-available but explicitly commercial/proprietary (requires a paid agreement, forbids redistribution/resale). So the project is best described as "op...

5 steps, stop=sufficient_info, 0 observed / 6 reported
$0.2033, 54s
```

```
QUESTION  Does microsoft/apm actually turn an apm.yml manifest into configured agent files with a single command, without network access at compile time?
REPO      microsoft/apm
CLAIM     One apm.yml manifest plus one command reproduces a project's agent configuration anywhere.

TRAJECTORY
  - step 1: get_repo_metadata({'repo': 'microsoft/apm'})
  - step 1: list_files({'repo': 'microsoft/apm', 'path': ''})
  - step 2: read_file({'repo': 'microsoft/apm', 'path': 'README.md'})
  - step 2: read_file({'repo': 'microsoft/apm', 'path': 'apm.yml'})
  - step 3: list_files({'repo': 'microsoft/apm', 'path': 'docs'})
  - step 3: list_files({'repo': 'microsoft/apm', 'path': 'src'})
  - step 4: list_files({'repo': 'microsoft/apm', 'path': 'src/apm_cli'})
  - step 5: sandbox_clone({'repo': 'microsoft/apm'})
  - step 6: sandbox_setup({'command': 'cd /work/repo && pip install -e . --quiet 2>&1 \| tail -30'})
  - step 7: sandbox_run({'command': 'export PATH=$PATH:/work/.local/bin && cd /tmp && rm -rf apmtest && mkdir apmt...)
  x step 8: sandbox_run({'command': 'cat /tmp/apmtest/AGENTS.md 2>/dev/null'})
  x step 9: sandbox_run({'command': 'find /tmp/apmtest -maxdepth 2 -type f'})

LEDGER
  [    for / reported] "Portable by manifest... one command reproduces it everywhere... Declare your project's agentic dependencies once in apm.yml, and every developer who clones your repo gets a fully configured agent setup in seconds"; zero-config example: `apm compile -t copilot` writes `.github/copilot-instructions.m...
            from: read_file(README.md)
  [    for / OBSERVED] Cloned repo, installed apm CLI via pip install -e ., then in a separate network-free sandbox run created a minimal apm.yml with one local instruction primitive; confirmed no network reachable; ran `apm compile -t copilot` which succeeded (exit 0) printing 'Compilation completed successfully!' and ge...
            from: sandbox_run(apm compile -t copilot)
  [unknown / reported] The repository's own apm.yml manifest uses local path dependencies (./packages/apm-issue-autopilot, ./packages/batch-bug-shepherd) rather than remote-only ones, consistent with compile operating purely on locally-resolved primitives without needing network access, while apm install would need networ...
            from: read_file(apm.yml)

FACTS
  setup          19.6s over 1 command(s), install succeeded
  disk           55 MB cloned, 150 MB total
  needs          nothing special
  exercised      apm compile -t copilot generates configured agent files (AGENTS.md, .github/copilot-instructions.md) from an apm.yml manifest in a single command with no network access required.
  output         Compilation completed successfully! (exit code 0), generating AGENTS.md and .github/copilot-instructions.md with correctly compiled instruction content.

VERDICT   SUPPORTED
SUMMARY   Direct sandbox testing confirmed that `apm compile -t copilot` reproduces a fully configured agent file from an apm.yml manifest in one command, with no network connectivity, matching the compile-time portion of the claim. Network is only needed for `apm install` when fetching new remote dependencies, not for compile itself.

10 steps, stop=sufficient_info, 1 observed / 2 reported
$0.3489, 79s
```

```
QUESTION  Does 0xPlaygrounds/rig actually build and run a working LLM agent from its own quickstart example?
REPO      0xPlaygrounds/rig
CLAIM     rig is a Rust library for building modular, scalable LLM applications.

TRAJECTORY
  - step 1: get_repo_metadata({'repo': '0xPlaygrounds/rig'})
  x step 1: sandbox_run({'command': 'which cargo rustc 2>&1; echo "---"; cargo --version 2>&1'})
  - step 2: sandbox_clone({'repo': '0xPlaygrounds/rig'})
  - step 3: sandbox_run({'command': 'which cargo rustc 2>&1; echo "exit:$?"'})
  - step 4: read_file({'repo': '0xPlaygrounds/rig', 'path': 'README.md'})
  - step 5: bash_code_execution({'command': 'cd /work/repo && grep -n "Simple example" -A 40 README.md \| head -60'}) [web]
  - step 5: sandbox_run({'command': 'cd /work/repo && grep -n "Simple example" -A 40 README.md \| head -60'})

LEDGER
  [unknown / reported] The repository's metadata reports language 'Rust', confirming this is a Rust project.
            from: get_repo_metadata(0xPlaygrounds/rig)
  [against / OBSERVED] `which cargo rustc` returned exit code 1 — no Rust toolchain (cargo/rustc) is present in the sandbox, and no toolchain was installed.
            from: sandbox_run(which cargo rustc)
  [    for / reported] README states Rig is a Rust library for building scalable, modular, and ergonomic LLM-powered applications.
            from: sandbox_run(grep README.md)
  [unknown / reported] The README content that was actually read (step 4) and grepped (step 5) was truncated before showing the quickstart code itself, so the specific quickstart example (client construction, agent build, prompt call) was not confirmed to exist in the retrieved text.
            from: read_file(README.md); sandbox_run(grep README.md)

FACTS
  setup          8.0s over 0 command(s), install not run/failed
  disk           118 MB cloned, 118 MB total
  needs          api_key, network_at_runtime
  exercised      Rust library for building scalable, modular, and ergonomic LLM-powered applications
  output         which cargo rustc -> exit code 1 (no output)

VERDICT   COULD_NOT_TEST
BLOCKED   unsupported_language, needs_api_key
SUMMARY   Rig is a Rust crate; the sandbox lacks a Rust toolchain (cargo/rustc) and none was installed, which blocks compiling any example. The README (partially read/grepped) confirms Rig's stated purpose as a Rust LLM library, but the specific quickstart code details were not actually surfaced in the evidence gathered — the README reads were truncated before reaching that section. Combined with the missing toolchain and the general expectation that Rig examples require a live OPENAI_API_KEY, the quickstart agent could not be built or run in this environment, and its exact contents were not verified ei...

6 steps, stop=sufficient_info, 1 observed / 3 reported
$0.2257, 49s
```
