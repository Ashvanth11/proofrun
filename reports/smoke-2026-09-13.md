# Investigation batch

3 questions, $1.17, 4 min.

Verdicts: 1 inconclusive, 1 none, 1 supported.

`Obs`/`Rep` are observed and reported ledger entries: observed means a command ran in the sandbox and printed it. `(v)` marks a verdict downgraded for lacking observed evidence.

| Question | Verdict | Obs | Rep | Blockers | Setup | Cost | Wall | Steps |
|---|---|---:|---:|---:|---:|---:|---:|---:|
| Is langfuse/langfuse actually open source under a recognised OSI licence, or is some of what its README advertises cover... | none | 0 | 0 | - | - | $0.146 | 53s | 5 |
| Does microsoft/apm actually turn an apm.yml manifest into configured agent files with a single command, without network ... | supported | 5 | 0 | - | 21s | $0.512 | 104s | 12 |
| Does 0xPlaygrounds/rig actually build and run a working LLM agent from its own quickstart example? | inconclusive | 2 | 5 | needs_api_key, other | 36s | $0.513 | 103s | 13 |

## Trajectories

```
QUESTION  Is langfuse/langfuse actually open source under a recognised OSI licence, or is some of what its README advertises covered by a separate commercial licence?
REPO      langfuse/langfuse
CLAIM     Langfuse is an open source AI engineering platform.

TRAJECTORY
  - step 1: get_repo_metadata({'repo': 'langfuse/langfuse'})
  - step 1: read_file({'repo': 'langfuse/langfuse', 'path': 'README.md'})
  - step 2: list_files({'repo': 'langfuse/langfuse', 'path': ''})
  - step 2: read_file({'repo': 'langfuse/langfuse', 'path': 'LICENSE'})
  - step 3: list_files({'repo': 'langfuse/langfuse', 'path': 'ee'})
  - step 4: read_file({'repo': 'langfuse/langfuse', 'path': 'ee/LICENSE'})
  - step 4: read_file({'repo': 'langfuse/langfuse', 'path': 'ee/README.md'})

LEDGER
  (empty)

VERDICT   none - the run produced no conclusion

5 steps, stop=sufficient_info, 0 observed / 0 reported
$0.1459, 53s
```

```
QUESTION  Does microsoft/apm actually turn an apm.yml manifest into configured agent files with a single command, without network access at compile time?
REPO      microsoft/apm
CLAIM     One apm.yml manifest plus one command reproduces a project's agent configuration anywhere.

TRAJECTORY
  - step 1: get_repo_metadata({'repo': 'microsoft/apm'})
  - step 1: list_files({'repo': 'microsoft/apm', 'path': ''})
  - step 2: read_file({'repo': 'microsoft/apm', 'path': 'README.md'})
  - step 2: read_file({'repo': 'microsoft/apm', 'path': 'apm.yml'})
  - step 3: sandbox_clone({'repo': 'microsoft/apm'})
  - step 4: sandbox_setup({'command': 'cd /work/repo && pip install -e . 2>&1 \| tail -30'})
  - step 5: sandbox_run({'command': 'export PATH=$PATH:/work/.local/bin\ncd /tmp && rm -rf testproj && mkdir testp...)
  - step 6: sandbox_run({'command': 'curl -s -m 3 https://github.com -o /dev/null -w "HTTP_CODE:%{http_code}\\n" \...)
  - step 7: sandbox_run({'command': 'export PATH=$PATH:/work/.local/bin\ncd /tmp/testproj\nmkdir -p .apm/instructi...)
  x step 8: sandbox_run({'command': 'export PATH=$PATH:/work/.local/bin\nmkdir -p /work/testproj && cd /work/testp...)
  x step 9: sandbox_run({'command': 'export PATH=$PATH:/work/.local/bin\ncd /work/testproj\necho "=== AGENTS.md ==...)
  - step 10: sandbox_run({'command': 'export PATH=$PATH:/work/.local/bin\ncd /work/repo\nls .apm .agents 2>/dev/nul...)
  - step 11: sandbox_run({'command': 'cd /work/repo\necho "=== confirm generated file exists ==="\nls -la .github/c...)

LEDGER
  [unknown / OBSERVED] Installed the CLI (pip install -e .) from the cloned repo and confirmed apm-cli 0.30.0 installed successfully.
            from: sandbox_setup
  [    for / OBSERVED] Confirmed the sandbox_run environment has no network: a curl to github.com returned HTTP code 000 (failure), and a Python DNS lookup for github.com raised socket.gaierror: Temporary failure in name resolution.
            from: sandbox_run
  [    for / OBSERVED] In a freshly created minimal test project containing only an apm.yml and a hand-written local .apm/instructions/*.instructions.md file, running apm compile -t copilot (no network) produced a generated AGENTS.md file containing the compiled instruction content, with exit code 0.
            from: sandbox_run
  [    for / OBSERVED] On the actual microsoft/apm repository (whose apm.yml declares targets: [copilot] and local package dependencies already materialized in .apm/), running the bare command apm compile (no network) reported 'Compiling for AGENTS.md + .github/copilot-instructions.md + .github/prompts/ + .github/agents/'...
            from: sandbox_run
  [    for / OBSERVED] Directly inspected the regenerated .github/copilot-instructions.md file afterward and it contained real compiled content (76 lines, e.g. project linting instructions pulled from .apm/ primitives), proving the command actually wrote a configured agent file rather than merely reporting success.
            from: sandbox_run

FACTS
  setup          20.9s over 1 command(s), install succeeded
  disk           55 MB cloned, 150 MB total
  needs          nothing special
  exercised      apm compile turns an apm.yml manifest (plus locally-materialized .apm/ primitives) into configured agent files (e.g. .github/copilot-instructions.md, AGENTS.md) via a single command, fully offline.
  output         Compiling for AGENTS.md + .github/copilot-instructions.md + .github/prompts/ + .github/agents/ Compilation completed successfully!

VERDICT   SUPPORTED
SUMMARY   microsoft/apm's apm compile command was verified to convert an apm.yml manifest plus locally materialized .apm/ primitives into configured agent files (e.g., .github/copilot-instructions.md, AGENTS.md) using a single command, with no network access required at compile time. This was confirmed both in a minimal test project and on the actual microsoft/apm repo itself, with network unavailability independently verified via failed curl and DNS lookups. The separate apm install command (fetching remote dependencies) does require network, but that is distinct from the compile step being tested.

12 steps, stop=sufficient_info, 5 observed / 0 reported, 3 entries dropped
$0.5121, 104s
```

```
QUESTION  Does 0xPlaygrounds/rig actually build and run a working LLM agent from its own quickstart example?
REPO      0xPlaygrounds/rig
CLAIM     rig is a Rust library for building modular, scalable LLM applications.

TRAJECTORY
  - step 1: get_repo_metadata({'repo': '0xPlaygrounds/rig'})
  - step 1: read_file({'repo': '0xPlaygrounds/rig', 'path': 'README.md'})
  - step 2: bash_code_execution({'command': 'echo "find readme quickstart section"'}) [web]
  x step 2: list_files({'repo': '0xPlaygrounds/rig', 'path': 'rig-core/examples'})
  - step 3: list_files({'repo': '0xPlaygrounds/rig', 'path': ''})
  - step 4: list_files({'repo': '0xPlaygrounds/rig', 'path': 'examples'})
  - step 5: list_files({'repo': '0xPlaygrounds/rig', 'path': 'examples/agent'})
  - step 5: read_file({'repo': '0xPlaygrounds/rig', 'path': 'examples/README.md'})
  - step 6: list_files({'repo': '0xPlaygrounds/rig', 'path': 'examples/agent/src'})
  - step 6: read_file({'repo': '0xPlaygrounds/rig', 'path': 'examples/agent/Cargo.toml'})
  - step 7: read_file({'repo': '0xPlaygrounds/rig', 'path': 'examples/agent/src/main.rs'})
  - step 8: sandbox_clone({'repo': '0xPlaygrounds/rig'})
  x step 9: sandbox_setup({'command': 'cd /work/repo && rustc --version && cargo --version'})
  - step 10: sandbox_setup({'command': "curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs \| sh -s -- -y --de...)
  - step 11: sandbox_setup({'command': "mkdir -p /work/tmpexec && cd /work/tmpexec && curl --proto '=https' --tlsv1.2...)
  x step 12: sandbox_setup({'command': 'source $HOME/.cargo/env && cd /work/repo && cargo build -p agent 2>&1 \| tail...)
  x step 13: sandbox_setup({'command': 'echo $HOME; find / -maxdepth 4 -iname "cargo" -type d 2>/dev/null; ls -la /ro...)

LEDGER
  [    for / reported] examples/README.md describes the 'agent' example as "Demonstrates the smallest useful agent setup with OpenAI."
            from: read_file
  [    for / reported] examples/agent/src/main.rs constructs an openai::Client::from_env(), builds an agent with a preamble and model GPT_4O, calls .prompt(PROMPT).await?.output, and prints the result — a complete, coherent, minimal working LLM-agent example.
            from: read_file
  [    for / reported] The example's Cargo.toml declares rig (workspace), anyhow, and tokio as dependencies with no unusual/missing pieces.
            from: read_file
  [    for / reported] README.md describes Rig generally as 'a Rust library for building scalable, modular, and ergonomic LLM-powered applications', matching the claim's wording.
            from: read_file
  [unknown / reported] Repository was cloned successfully.
            from: sandbox_clone
  [    for / OBSERVED] Rust toolchain (rustc/cargo) was installed successfully via rustup.
            from: sandbox_setup
  [unknown / OBSERVED] The subsequent `cargo build -p agent` invocation failed because `$HOME/.cargo/env` was not found at the expected path, a shell/environment issue rather than a code issue.
            from: sandbox_setup

FACTS
  setup          36.3s over 4 command(s), install not run/failed
  disk           116 MB cloned, 116 MB total
  needs          api_key, network_at_runtime
  exercised      Demonstrates the smallest useful agent setup with OpenAI

VERDICT   INCONCLUSIVE
BLOCKED   needs_api_key, other
SUMMARY   Code inspection shows a complete, plausible minimal LLM agent example (openai client + agent + prompt call) matching the README's description of Rig, but the investigator could not empirically verify it builds or runs: the sandbox ran out of setup calls after a Rust toolchain install succeeded but before `cargo build -p agent` could be executed (blocked by a $HOME/.cargo/env path issue), and a live run would additionally require an OpenAI API key and network access that were not available.

13 steps, stop=sandbox_cap, 2 observed / 5 reported
$0.5128, 103s
```
