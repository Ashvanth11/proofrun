venv/
__pycache__/
*.pyc
.env
*.db
*.sqlite
*.sqlite3
# Local briefs stay untracked; the scheduled workflow force-adds them in CI.
reports/*.md
# Generated weekly briefs are noise; eval and smoke reports are evidence and
# are committed - every number in the README has to trace back to one.
!reports/eval-investigations-*.md
!reports/smoke-*.md
!reports/.gitkeep
.pytest_cache/
.DS_Store

# Planning notes, written to myself rather than to a reader.
ROADMAP-*.md

# Notes to myself about how to talk about this, not documentation.
docs/resume-line.md

# Durable scheduled-run state is an Actions artifact, never a public site asset.
weekly-state/
